program ecran;

{$mode objfpc}{$H+}

uses
  {$IFDEF UNIX}{$IFDEF UseCThreads}
  cthreads,
  {$ENDIF}{$ENDIF}
  Classes, debuterJeu, gestionnaire, uniteMine, Unitforet, tableCraft,
  unitMarchand, croisementLieu, GestionEcran, house, derriereMaison, unitBamboo,
  unitPanda, debutFin;

var
  quitter : boolean;

begin
  ecranTitre;
  debutJeu;
end.

